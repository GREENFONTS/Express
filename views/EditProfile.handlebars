
<div class='col-md-7 m-auto'> {{> error}}</div>
<div class="container">
    
    
    <h3 class="text-center mb-3">Edit your Profile</h3>

<form  class="  card card-body col-md-7 m-auto" action="/user/EditProfile" method="POST" enctype="application/x-www-form-urlencoded">
    <div class="form-group">
        <label for="name">UserName</label>
        <input type="name" id="name" name="Name" class="form-control" value="{{Name}}" placeholder="Enter your UserName"
            required />
    </div>
    <div class="form-group">
        <label for="email">Email</label>
        <input type="email" id="email" name="Email" class="form-control" value="{{Email}}" placeholder="Enter Email"
            required />
    </div>

    <div class="form-group">
        <label for="email">Occupation</label>
        <input type='text' id="Ocupation" name="Occupation" class="form-control" value="{{Occupation}}" placeholder="Occupation" required />
    </div>
    <div class="form-group">
        <label for="Skills">Skills</label>
        <input type="text" name="Skills" class="form-control" required />
    </div>

    <div class="form-group">
        <label for="Hobbies">Hobbies</label>
        <input type="text" id="Hobbies" name="Hobbies" class="form-control" placeholder="What you like doing (Hobbies)"
            required />
    </div>
    <div class="form-group">
        <label for="AboutMe">Description</label>
        <textarea name="About" class="form-control" placeholder="Descirbe yourself in not less than 200 words"
            required ></textarea>
    </div>
    <button type="submit" class="btn btn-primary btn-block">
        Submit
    </button>
</form>
</div>